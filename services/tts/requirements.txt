# Requirements for NovelLabs TTS Service - Modal Deployment
# ============================================================
# These are installed automatically in the Modal container.
# Install locally for development: pip install -r requirements.txt

# Core dependencies
modal>=0.64.0
fastapi[standard]==0.115.4
pydantic>=2.0.0

# TTS and audio processing
kokoro-onnx>=0.1.0
numpy>=1.26.0,<2.0.0
soundfile>=0.12.0
torch>=2.1.0,<3.0.0

# Cloud storage
boto3>=1.34.0
botocore>=1.34.0

# Development tools (optional)
uvicorn[standard]>=0.30.0  # For local testing
httpx>=0.27.0              # For testing API calls
pytest>=8.0.0              # For unit tests
pytest-asyncio>=0.23.0     # For async tests

# Note: Modal handles dependency installation in the cloud.
# You only need to install these locally if you want to:
# 1. Test the synthesis logic locally (without Modal)
# 2. Run unit tests
# 3. Develop/debug the code locally