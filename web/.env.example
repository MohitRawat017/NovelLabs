# Frontend environment example
# Copy to .env.local for local development or set these in your frontend host.

# Backend API URL (must include /api)
VITE_API_URL=http://localhost:8001/api

# false for your local writable setup, true for the deployed reader-only frontend
VITE_READ_ONLY_MODE=false
